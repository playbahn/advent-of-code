// use std::collections::HashSet;

// const INPUT: &str = "input/day-23.txt";

fn main() {
    // let mut conn_sets: Vec<HashSet<String>> = vec![HashSet::with_capacity(3); 676];

    // std::fs::read_to_string(INPUT)
    //     .unwrap()
    //     .lines()
    //     .for_each(|conn| {
    //         let n1 = &conn[..2];
    //         let n2 = &conn[3..];
            
    //         let mut contains = false;
            
    //         for conn_set in &mut conn_sets {
    //             // if conn_set.contains(n1) || 
    //         }
    //     });
}
